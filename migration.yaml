trigger: none

pool:
  name: 'Batch13pool'
  demands:
  - Agent.Name -equals akagent2

variables:
  # Azure
  azureServiceConnection: 'batch13-serviceconnection'
  storageAccount: 'akstgacc'
  storageContainer: 'adocont'

  # SonarQube
  sonarQubeConnection: 'Sonarqubeconnection-ado-new'
  sonarProjectKey: 'Akshata-sq'
  sonarProjectName: 'Akshata-sq'

  # Repository directories
  BACKEND_DIR: 'backend'
  FRONTEND_DIR: 'frontend'
  ELECTRON_DIR: 'electron'
  TERRAFORM_DIR: 'Terraform'

steps:

# =====================================================
# 1. CHECKOUT
# =====================================================
- checkout: self
  clean: true
  displayName: 'Checkout Repository'


# =====================================================
# 2. CHECK REQUIRED TOOLS
# =====================================================
- script: |
    echo ===== Checking Required Tools =====
    java -version
    mvn -version
    node --version
    npm --version
    git --version
    C:\gitleaks\gitleaks.exe version
  displayName: 'Check Required Tools'


# =====================================================
# 3. GITLEAKS SECURITY SCAN
# =====================================================
- script: |
    echo ===== Gitleaks Security Scan =====

    C:\gitleaks\gitleaks.exe detect ^
      --source="$(Build.SourcesDirectory)" ^
      --config="$(Build.SourcesDirectory)\.gitleaks.toml" ^
      --verbose

  displayName: 'Security Scan - Gitleaks'


# =====================================================
# 4. SONARQUBE PREPARE - MAVEN MODE
# =====================================================
- task: SonarQubePrepare@7
  displayName: 'Prepare SonarQube Analysis'
  inputs:
    SonarQube: '$(sonarQubeConnection)'
    scannerMode: 'other'
    extraProperties: |
      sonar.projectKey=$(sonarProjectKey)
      sonar.projectName=$(sonarProjectName)


# =====================================================
# 5. TEST + BUILD + SONARQUBE ANALYSIS
# =====================================================
- task: Maven@4
  displayName: 'Test Build and Analyze Backend'
  inputs:
    mavenPOMFile: '$(BACKEND_DIR)/pom.xml'
    goals: 'clean verify'
    options: '-DskipTests=false'
    publishJUnitResults: true
    testResultsFiles: '**/surefire-reports/TEST-*.xml'
    javaHomeOption: 'Path'
    jdkDirectory: '$(JAVA_HOME)'
    mavenVersionOption: 'Default'
    sonarQubeRunAnalysis: true


# =====================================================
# 6. VERIFY JAVA BUILD
# =====================================================
- powershell: |
    Write-Host "===== Verifying Java Build ====="

    $classesPath = "$(Build.SourcesDirectory)\backend\target\classes"

    if (!(Test-Path $classesPath)) {
        Write-Error "Compiled classes directory not found: $classesPath"
        exit 1
    }

    $classFiles = Get-ChildItem `
      -Path $classesPath `
      -Filter *.class `
      -Recurse `
      -ErrorAction SilentlyContinue

    if ($classFiles.Count -eq 0) {
        Write-Error "No compiled Java .class files found."
        exit 1
    }

    Write-Host "SUCCESS: Found $($classFiles.Count) compiled Java classes."

  displayName: 'Verify Java Build Output'


# =====================================================
# 7. COPY BACKEND DEPENDENCIES
# =====================================================
- script: |
    echo ===== Copying Maven Dependencies =====

    mvn dependency:copy-dependencies ^
      -DoutputDirectory=target/dependency

  workingDirectory: '$(BACKEND_DIR)'
  displayName: 'Copy Backend Dependencies'


# =====================================================
# 8. BUILD FRONTEND
# =====================================================
- script: |
    echo ===== Building React Frontend =====

    call npm install
    call npm run build

  workingDirectory: '$(FRONTEND_DIR)'
  displayName: 'Build Frontend'


# =====================================================
# 9. SONARQUBE QUALITY GATE
# =====================================================
- task: SonarQubePublish@7
  displayName: 'Publish SonarQube Quality Gate'
  inputs:
    pollingTimeoutSec: '600'


# =====================================================
# 10. PREPARE BUILD ARTIFACT
# =====================================================
- task: CopyFiles@2
  displayName: 'Prepare Build Artifact'
  inputs:
    SourceFolder: '$(Build.SourcesDirectory)'
    Contents: |
      **/*
      !**/.git/**
      !**/node_modules/**
    TargetFolder: '$(Build.ArtifactStagingDirectory)'


# =====================================================
# 11. PUBLISH PIPELINE ARTIFACT
# =====================================================
- task: PublishPipelineArtifact@1
  displayName: 'Publish Build Artifact'
  inputs:
    targetPath: '$(Build.ArtifactStagingDirectory)'
    artifact: 'drop'
    publishLocation: 'pipeline'


# =====================================================
# 12. UPLOAD ARTIFACT TO AZURE STORAGE
# =====================================================
- task: AzureCLI@2
  displayName: 'Upload Artifact to Azure Storage'
  inputs:
    azureSubscription: '$(azureServiceConnection)'
    scriptType: 'batch'
    scriptLocation: 'inlineScript'
    inlineScript: |
      echo ===== Uploading Artifact to Azure Storage =====

      az storage blob upload-batch ^
        --account-name "$(storageAccount)" ^
        --destination "$(storageContainer)" ^
        --source "$(Build.ArtifactStagingDirectory)" ^
        --auth-mode login ^
        --overwrite


# =====================================================
# 13. VERIFY AZURE STORAGE
# =====================================================
- task: AzureCLI@2
  displayName: 'Verify Azure Storage Upload'
  inputs:
    azureSubscription: '$(azureServiceConnection)'
    scriptType: 'batch'
    scriptLocation: 'inlineScript'
    inlineScript: |
      echo ===== Verifying Azure Blob Storage =====

      az storage blob list ^
        --account-name "$(storageAccount)" ^
        --container-name "$(storageContainer)" ^
        --auth-mode login ^
        --output table


# =====================================================
# 14. SUCCESS
# =====================================================
- script: |
    echo ==========================================
    echo Pipeline Completed Successfully
    echo ==========================================
  displayName: 'Pipeline Success'
  condition: succeeded()